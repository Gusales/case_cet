[pytest]
pythonpath = .
testpaths = tests

markers =
    unit: Testes unitários
    e2e: testes de ponta a ponta
    persona: Cenários das personas documentadas em docs/PERSONAS.md

addopts =
    -v
    --strict-markers
    --tb=short